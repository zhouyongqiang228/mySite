# 周永强 · 个人求职网站

纯 HTML、CSS、JavaScript，重点展示医学影像程序开发、AI 应用、自动化工具三个求职方向。

## 预览

```sh
python3 -m http.server 5174 --bind 127.0.0.1
```

访问 http://localhost:5174/ 。技能球脚本与技能目录版本标记为 v13，其余资源为 v12。

## 内容与来源

- 工作经历替换原有示例，按用户本次提供的信息与旧 PDF 整理。
- 工作和项目时间统一按年展示，原始月份保留在独立核对资料中；旧 PDF 与后续说明的差异仍需核对。
- 拟仁公司名采用本次提供的写法；其法定名称和渲染引擎来源尚待核对，正文只描述图形渲染集成工作。
- 项目包括 AutoAct、Window Fusion、休闲游戏、AI 低代码游戏平台、神笔马良 AI 游戏、社交软件、数据分析平台、BOSS 求职辅助扩展、自研 2D / 3D 引擎及类 C 解释型语言。
- 医学影像写为求职方向，不宣称有未经确认的临床交付、医疗资质或效果指标。
- 未采用旧简历的过时年龄、工龄、期望薪资，以及缺少佐证的榜单与下载成绩。保留有项目支撑的技能、职责、教育与竞赛经历。
- 参考 PDF 原文及核对笔记单独保存在 `/Users/alex/Documents/ResumeResearch`，不包含在网站内。线上 BOSS 原简历尚未提取成功。

## 维护

- `index.html`：履历、项目、求职方向、教育与竞赛信息。
- `skill-data.js`：技能球目录，包括 DirectX、OpenGL、ASO、移动游戏推广。
- `skill-tree.js` / `skill-tree.css`：旋转技能球。
- `styles.css`：响应式和打印布局。
- `script.js`：导航菜单、当前栏目提示、页脚年份。
- `assets/`：真实项目截图和视频封面；素材来源记录在独立核对笔记中。

## 星球抖动修复

原算法每帧从投影锚点重新进行九轮标签碰撞推开。当两个标签相交时，推开方向及轴会突然变化；后续固定系数插值不能消除这种目标跳变。持续缩放文字还会引起字形重采样。

现在标签直接跟随连续投影轨迹，固定文字尺寸，通过平滑透明度表示前后层次；不再逐帧更改 left/top 或动态交换 z-index。前后标签交会时允许短暂重叠，后方标签降低亮度，完整技能另在静态技术能力区呈现。

球半径预留标签边界空间，避免位置硬限位导致速度突变。同时避免相同尺寸的 ResizeObserver 重复初始化；字体加载完成后重测尺寸。保留约 42 秒一圈的旋转、减少动态效果、离屏暂停和后台暂停。

## 验证

```sh
node --check script.js
node --check skill-data.js
node --check skill-tree.js
git diff --check
```

## AutoAct 官网与子路径

已定位官网源码：`/usr/local/go/src/Servers/serverAll/main`。`main.go` 使用 Go 标准库 `net/http` 注册 `/index`、`/download` 等路由；`handle_download.go` 直接提供 `client/static/autoact/download.html`。修改前下载页与 `https://autoact.app/download` 返回内容的 SHA-1 相同。

本次修改该下载页为“暂时不提供下载”，移除两个安装包链接。只修改本地文件，尚未发布到服务器。

简历可挂载到 `https://autoact.app/zyq`。部署时将本项目的 HTML、CSS、JS、favicon 和 assets 放入专用静态目录，由 Go 注册 `/zyq/` 静态路由，并将 `/zyq` 重定向到 `/zyq/`，使相对资源地址正常解析。仅发布网页文件，不包含 README、Git 历史或履历核对笔记。本次仅确认可行性，未修改 Go 路由或部署简历。

## 本次内容精简

- 页面姓名统一为周永强；教育信息改为湖州师范大学。
- 工作、项目时间以年显示，同年起止只显示一次年份。
- 删除“让下一个项目，从一次交流开始”整个板块，以及关联导航和按钮脚本。
- 将模板化栏目口号改成直接标题，去掉缺少个人项目细节的“我的工作思路”四步流程及泛泛自述。
- 去掉姓名上方重复标语、装饰性英文副标题、技能球序号和底部动态计数；保留技能节点与旋转效果。
- 额外建议精简：重复能力概览、关于我的大字装饰与签名；未获用户选择前保留。

本次实际 Chrome 检查：简历在 1440、768、390、320px 下无横向溢出，移动导航正常；下载页在 1440、390、320px 下正常。页面无 JavaScript 运行错误；7 段工作经历、10 个项目、51 个技能节点保留，锚点检查通过。
